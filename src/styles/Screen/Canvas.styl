@import "nib"
@import "../common"

resize-area-size = 4px

.canvas
  overflow: hidden

.canvas__mask
  position: fixed
  display: none
  .canvas--active > &
    background: rgba(color-accent, 0.5)
    display: block
  .canvas--selected > &
    border: 1px solid color-accent
    display: block

$canvas-mask-resize-area
  position: absolute

.canvas__mask-n
  @extend $canvas-mask-resize-area
  top: resize-area-size * -0.5
  left: resize-area-size
  right: resize-area-size
  height: resize-area-size
  cursor: n-resize

.canvas__mask-e
  @extend $canvas-mask-resize-area
  top: resize-area-size
  right: resize-area-size * -0.5
  bottom: resize-area-size
  width: resize-area-size
  cursor: e-resize

.canvas__mask-s
  @extend $canvas-mask-resize-area
  bottom: resize-area-size * -0.5
  left: resize-area-size
  right: resize-area-size
  height: resize-area-size
  cursor: s-resize

.canvas__mask-w
  @extend $canvas-mask-resize-area
  top: resize-area-size
  left: resize-area-size * -0.5
  bottom: resize-area-size
  width: resize-area-size
  cursor: w-resize

$canvas-mask-resize-corner
  @extend $canvas-mask-resize-area
  width: resize-area-size
  height: resize-area-size
  background: color-accent

.canvas__mask-ne
  @extend $canvas-mask-resize-corner
  top: resize-area-size * -0.5
  right: resize-area-size * -0.5
  cursor: ne-resize

.canvas__mask-se
  @extend $canvas-mask-resize-corner
  bottom: resize-area-size * -0.5
  right: resize-area-size * -0.5
  cursor: se-resize

.canvas__mask-sw
  @extend $canvas-mask-resize-corner
  bottom: resize-area-size * -0.5
  left: resize-area-size * -0.5
  cursor: sw-resize

.canvas__mask-nw
  @extend $canvas-mask-resize-corner
  top: resize-area-size * -0.5
  left: resize-area-size * -0.5
  cursor: nw-resize

.canvas__screen
  position: absolute
  top: 0
  left: 0
  right: 0
  bottom: 0
  margin: auto
  width: 360px
  height: 640px
  background: #fff

.canvas__text
  color: #000